/*
Program to find sum of even & odd numbers
from a given list of numbers
*/

#include <stdio.h>
#include <ctype.h>

int main(void){
    int num = 10;
    char ch = '9';
    if(isdigit(ch)){
        printf("%d is a digit", ch);
    }else{
        printf("%d is not a digit", ch);
    }
}